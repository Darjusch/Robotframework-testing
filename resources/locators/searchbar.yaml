# Searchbar
order searchbar input field: css=input[placeholder*="Search"] # Search input field
reset searchbar button: css=button[title*="Reset"] # Reset search button
no_matching_orders_banner: role=heading[name*="No matching order"]
